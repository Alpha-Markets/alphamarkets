{
  "name": "@orionis/hedger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Orionis Markets hedger: keeps a wallet's options book delta neutral with perp positions (PROJECT_BRIEF.md Section 40, automated hedging tools). Dry run unless told to trade.",
  "scripts": {
    "build": "echo \"services/hedger runs from source (tsx)\" && exit 0",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "lint": "echo \"scaffold: services/hedger lint\" && exit 0",
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@orionis/config": "workspace:*",
    "@orionis/sdk": "workspace:*",
    "viem": "^2.21.0"
  }
}
