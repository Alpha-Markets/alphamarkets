{
  "name": "@orionis/sdk",
  "private": true,
  "version": "0.0.0",
  "description": "Orionis Markets typed client — the only sanctioned way to talk to contracts/API from any frontend or integrator.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"scaffold: packages/sdk build — Phase 3\" && exit 0",
    "lint": "echo \"scaffold: packages/sdk lint — Phase 3\" && exit 0",
    "test": "echo \"scaffold: packages/sdk test — Phase 3\" && exit 0",
    "typecheck": "echo \"scaffold: packages/sdk typecheck — Phase 3\" && exit 0"
  }
}
